# === Jekyll build output & caches ===
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# === Ruby / Bundler ===
.bundle/
vendor/

# === Stray artifacts that shipped in the original archive ===
# (literal "$HOME" folder containing a pre-vendored gem bundle)
$HOME/

# === Python (helper scripts in markdown_generator/) ===
venv/
.venv/
__pycache__/
*.py[cod]

# === Node / npm (only if you add tooling) ===
node_modules/
package-lock.json

# === Local-only / deployment ===
local/
.elasticbeanstalk/config.yml

# === Editor / OS files ===
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini
